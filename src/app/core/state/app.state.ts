// src/app/state/app.state.ts

/**
 * Root application state.
 * Add feature state slices here as they grow.
 */
export interface AppState {
    // example:
    // textNotes: TextNotesState;
    // articleNotes: ArticleNotesState;
  }
  